// All constants have been moved to @pocket-pixie/db-schema
// Import them from there instead

import { initialAccountSeed } from "@pocket-pixie/db-schema";

// Re-exporting for backwards compatibility
export { initialAccountSeed };
